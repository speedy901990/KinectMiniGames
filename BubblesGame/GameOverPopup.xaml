<UserControl x:Class="BubblesGame.GameOverPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:k="http://schemas.microsoft.com/kinect/2013" 
             mc:Ignorable="d" 
             Width="300" Height="300"
             BorderBrush="#FF4E3876" BorderThickness="5" Loaded="UserControl_Loaded_1">
    <Grid Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Koniec Gry!" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="50"/>
        <TextBlock x:Name="tblResult" Grid.Row="1" Width="300" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32" MaxWidth="300" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" TextAlignment="Center">Wszystkie jabłka zostały wrzucone!</TextBlock>
    </Grid>
</UserControl>